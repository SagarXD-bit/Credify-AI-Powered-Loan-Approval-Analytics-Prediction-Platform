kind = "web"
previewPath = "/"
title = "Loan Approval Analytics"
version = "1.0.0"
id = "artifacts/loan-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19058

[services.development]
run = "bash -c 'cd /home/runner/workspace/artifacts/loan-app && streamlit run app.py --server.port 19058 --server.headless true --server.address 0.0.0.0'"

[services.env]
PORT = "19058"
